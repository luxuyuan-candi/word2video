# Word2Video PC Web 应用页面功能设计

## 1. 文档目标

本文档用于定义 Word2Video PC Web 应用的首版页面功能设计，作为后续前端开发、接口联调、交互验收和任务拆分的基础。

当前产品重点不是直接生成最终视频文件，而是协助用户以“项目”为单位，把一个或多个文字剧本持续转化为可用于 AI 视频生产的结构化素材包，包括项目级知识图谱、实体对象模型图、视频帧文字描述和帧级参考图片关联。

后续任何功能规则、页面流程、数据结构或导出格式的调整，都需要同步更新本文档，确保产品设计文档始终作为当前功能实现的事实来源。

## 2. 产品定位

Word2Video 是一个面向 PC 用户的 AI 视频前期制作工具。用户创建项目后，可以在同一个项目下持续新增剧本。平台会基于项目内的剧本内容持续更新知识图谱，提取人物、物品、场景等实体对象，并引导用户为这些实体对象生成或上传参考图片。

最终，平台输出一份结构化创作资产：项目级知识图谱、项目级实体对象模型图片、按用户所选剧幕生成的视频分帧文字描述，以及每一帧关联的实体参考图。用户可以把这些内容带到其他 AI 图像或视频平台中继续生成画面和视频。

## 3. 目标用户

- AI 视频创作者：需要把剧本拆解成可执行的画面生成素材。
- 短片/故事创作者：希望保证人物、物品和场景在多帧画面中保持一致。
- 内容运营人员：需要批量把文案、广告脚本、口播脚本整理为视频生成提示词。
- 教育/培训内容生产者：需要把课程脚本拆分为清晰的画面描述。
- AI 工作流用户：熟悉 Midjourney、Stable Diffusion、Runway、可灵、即梦等外部生成工具，需要更稳定的前期素材管理。

## 4. 核心业务流程

```text
创建或打开项目
  ↓
输入或追加文字剧本
  ↓
平台解析剧本内容
  ↓
更新项目级知识图谱并编织对象关系
  ↓
识别并合并人、物、场景等实体对象
  ↓
引导用户为项目级实体对象生成或上传参考图片
  ↓
沉淀项目级知识图谱与实体对象模型图片
  ↓
按用户选择的一个或多个剧幕划分视频帧文字描述
  ↓
为每一帧关联相关实体参考图
  ↓
按用户选择的一个或多个剧幕导出结构化素材包，供外部平台继续生成画面
```

## 5. 首版功能范围

### 5.1 包含功能

- 文字剧本输入与编辑。
- 项目级剧本管理，支持同一项目下新增多个剧本。
- 剧本解析任务提交。
- 项目级知识图谱生成、增量更新与可视化展示。
- 实体对象识别，包含人物、物品、场景等类型。
- 实体对象跨剧本合并，避免同一项目下重复生成同一人物、物品或场景。
- 实体对象详情查看与编辑。
- 引导用户为实体对象生成模型参考图片。
- 支持用户输入图片生成提示词。
- 支持用户上传自己的参考图片。
- 实体图片生成结果预览、选择和重新生成。
- 支持单选、多选、全选剧幕后分帧，生成每一帧的文字描述。
- 每一帧关联相关实体对象和实体参考图片。
- 支持单选、多选、全选剧幕后导出最终素材包。
- 导出素材以帧为粒度组织，每个帧目录使用数字编号命名，并包含帧场景描述和相关实体对象图片。
- 项目列表、项目创建、项目删除、项目重命名和项目详情恢复。

### 5.2 暂不包含功能

- 直接生成最终视频文件。
- 复杂时间线剪辑。
- 音频、配音和字幕合成。
- 多人协作。
- 商业化额度、支付和会员系统。
- 移动端专项适配。

## 6. 信息架构

```text
Word2Video
├── 项目
│   ├── 项目创建
│   ├── 项目重命名
│   ├── 项目删除
│   └── 项目列表
├── 项目工作台
│   ├── 剧本管理
│   │   ├── 剧幕列表
│   │   ├── 增加幕
│   │   └── 删除幕
│   ├── 解析进度
│   ├── 项目级知识图谱
│   ├── 项目级实体对象
│   ├── 项目级实体参考图
│   ├── 视频分帧
│   │   └── 按选中剧幕分帧
│   └── 素材包导出
│       └── 按选中剧幕导出
└── 项目详情
    ├── 原始剧本
    ├── 知识图谱
    ├── 实体资产
    ├── 分帧描述
    └── 导出记录
```

## 7. 页面设计

### 7.1 项目工作台页

页面目标：让用户在一个页面内完成从剧本输入到素材包导出的核心流程。

建议路径：`/`

#### 页面布局

PC 端采用三栏工作台布局：

- 左侧：流程步骤导航和项目状态。
- 中间：当前步骤的主要编辑区。
- 右侧：上下文预览、详情面板和操作建议。

推荐布局：

- 左侧宽度：240px。
- 中间区域：自适应主工作区。
- 右侧宽度：360px 到 420px。
- 页面最小适配宽度：1366px。

#### 步骤导航

| 步骤 | 说明 |
| --- | --- |
| 1. 剧本管理 | 用户在项目下增加、删除和编辑剧幕 |
| 2. 解析图谱 | 平台解析项目下全部剧幕，并重新生成或更新项目级知识图谱 |
| 3. 确认实体 | 用户查看并修正项目级人物、物品、场景等实体 |
| 4. 生成模型图 | 用户为项目级实体对象生成或上传参考图片 |
| 5. 生成分帧 | 用户单选、多选或全选剧幕后生成视频帧文字描述 |
| 6. 导出素材 | 用户单选、多选或全选剧幕后导出帧级素材目录 |

### 7.2 剧本管理页/步骤

页面目标：以项目为单位管理多个剧幕，支持增加幕、删除幕、编辑剧本标题和剧本文字。

#### 核心模块

| 模块 | 功能说明 |
| --- | --- |
| 剧幕列表 | 展示同一项目下的所有剧幕，支持切换当前编辑剧幕 |
| 增加幕 | 在当前项目下新增一幕 |
| 删除幕 | 删除当前选中的剧幕 |
| 剧本标题 | 每一幕独立标题，可编辑；修改后剧幕列表内容需要实时同步 |
| 剧本文字 | 当前剧幕的正文编辑区，支持输入、粘贴、清空、字数统计 |

#### 输入校验

| 字段 | 规则 |
| --- | --- |
| 剧本标题 | 可选，缺省时自动生成 |
| 剧本文字 | 建议 100 到 10000 字 |

#### 页面状态

| 状态 | 页面表现 |
| --- | --- |
| 初始状态 | 项目为空时提示用户先创建项目 |
| 已有项目状态 | 展示剧幕列表和当前剧幕编辑器 |
| 可解析状态 | 项目下至少存在一幕剧本后，解析按钮可点击 |
| 解析中 | 展示“分析全部剧幕、抽取实体、合并已有实体、更新关系”等阶段 |
| 解析失败 | 展示失败原因和重新解析按钮 |

#### 项目级素材共用规则

- 同一项目下的所有剧本共用一份知识图谱。
- 同一项目下的所有剧本共用一份实体对象清单。
- 同一项目下的所有剧本共用实体模型图片，避免同一人物、物品、场景重复生成参考图。
- 解析剧本时，平台应解析项目下所有剧幕，并基于已有知识图谱和实体资产做更新。
- 当新增剧本识别到疑似已有实体时，应优先合并到已有实体，并允许用户手动拆分或合并。
- 剧幕用于分段管理剧本内容；分帧和导出时通过单选、多选或全选剧幕确定处理范围。

### 7.3 知识图谱页/步骤

页面目标：把项目中所有剧幕沉淀出的对象、事件和关系可视化，帮助用户理解项目级知识图谱。点击“解析剧本”后，平台解析全部剧幕，并重新生成或更新项目级知识图谱。

#### 核心模块

| 模块 | 功能说明 |
| --- | --- |
| 图谱画布 | 展示实体节点和关系连线 |
| 更新标识 | 标记新增节点、新增关系、更新过的实体描述 |
| 节点筛选 | 按人物、物品、场景、事件筛选 |
| 节点搜索 | 快速定位某个实体对象 |
| 图谱详情面板 | 点击节点后展示实体信息和相关关系 |
| 关系说明 | 展示对象之间的关系类型和来源文本 |

#### 图谱节点类型

| 类型 | 示例 |
| --- | --- |
| 人物 | 主角、配角、旁白角色 |
| 物品 | 道具、装备、产品、关键物件 |
| 场景 | 城市、房间、学校、办公室、自然环境 |
| 事件 | 相遇、冲突、转折、行动、结尾 |
| 概念 | 品牌、主题、情绪、风格关键词 |

#### 主要交互

1. 用户查看图谱整体结构。
2. 点击节点，右侧展示该对象的描述、关系和出现位置。
3. 用户可修改实体名称、类型、描述。
4. 用户可删除误识别实体，或手动新增实体。
5. 用户可查看全部剧幕对项目图谱的影响。
6. 用户确认图谱后进入实体对象确认步骤。

#### 增量更新规则

- 点击“解析剧本”后，系统解析项目下全部剧幕，并重新生成或更新项目级图谱。
- 已存在的实体节点应保留原有 ID、主图和用户编辑过的描述。
- 新增关系需要记录来源剧本和来源文本，方便用户追溯。
- 当系统无法判断两个实体是否为同一对象时，应标记为“待确认合并”。

### 7.4 实体对象确认页/步骤

页面目标：让用户确认项目中需要保持视觉一致性的实体对象。实体对象是项目级资产，供同一项目下所有剧本共用。

#### 核心模块

| 模块 | 功能说明 |
| --- | --- |
| 实体列表 | 按类型展示人物、物品、场景等对象 |
| 实体卡片 | 展示名称、类型、描述、出现剧本数、出现次数、图片状态 |
| 实体编辑面板 | 修改实体名称、类型、外观描述、补充说明 |
| 合并建议 | 展示疑似重复实体，支持用户确认合并或保留 |
| 批量操作 | 批量确认、批量忽略、按类型筛选 |

#### 实体卡片状态

| 状态 | 说明 |
| --- | --- |
| 待确认 | 平台识别到但用户未确认 |
| 已确认 | 用户确认该实体需要进入后续生成 |
| 已忽略 | 用户认为该实体不需要生成参考图 |
| 待生成图片 | 实体已确认但没有参考图片 |
| 已有图片 | 实体已绑定至少一张参考图片 |
| 待确认合并 | 系统认为该实体可能与已有实体重复，需要用户确认 |

### 7.5 实体参考图生成页/步骤

页面目标：引导用户为非“概念”的实体对象生成或上传模型参考图片。“概念”类实体只用于图谱和提示词理解，不进入模型图生成列表。

#### 页面布局

- 左侧：非“概念”的实体对象列表和生成状态。
- 中间：当前实体的图片生成工作区。
- 右侧：实体描述、提示词、参考图片和生成历史。

#### 核心模块

| 模块 | 功能说明 |
| --- | --- |
| 当前实体信息 | 展示实体名称、类型、剧本描述和关系摘要 |
| 提示词编辑器 | 用户可输入或修改图片生成提示词 |
| 自动提示词建议 | 平台根据实体描述生成提示词草稿 |
| 参考图片上传 | 用户上传自己的图片作为参考 |
| 图片生成按钮 | 调用 AI 图片生成能力 |
| 生成结果画廊 | 展示多张候选图片 |
| 图片预览 | 放大查看图片细节 |
| 选为模型图 | 将某张图片设为该实体的主参考图 |
| 重新生成 | 基于当前提示词再次生成 |

#### 实体类型生成重点

| 实体类型 | 生成重点 |
| --- | --- |
| 人物 | 年龄、性别、服装、发型、表情、身份、风格一致性 |
| 物品 | 形状、材质、颜色、品牌感、使用场景 |
| 场景 | 空间结构、时间、光线、氛围、地点特征 |
| 概念 | 视觉符号、色彩、情绪和抽象表达 |

#### 主要交互

1. 用户选择一个待生成图片的实体。
2. 平台根据实体描述生成默认提示词。
3. 用户可以修改提示词。
4. 用户可以上传参考图片。
5. 用户点击生成图片。
6. 平台返回候选图片。
7. 用户预览候选图片，并选择一张作为实体模型图。
8. 用户可继续为下一个实体生成图片。

### 7.6 视频分帧页/步骤

页面目标：用户单选、多选或全选剧幕后，按所选剧幕生成视频帧文字描述，并为每一帧关联项目级实体对象和参考图片。

#### 核心模块

| 模块 | 功能说明 |
| --- | --- |
| 剧幕选择 | 展示剧幕列表，支持单选、多选、全选 |
| 分帧列表 | 按顺序展示每一帧 |
| 帧描述 | 展示该帧的画面文字描述 |
| 镜头信息 | 展示景别、镜头运动、视角、画面氛围等 |
| 关联实体 | 展示该帧涉及的项目级人物、物品、场景 |
| 参考图片 | 展示关联实体的模型图片 |
| 提示词复制 | 一键复制该帧画面生成提示词 |
| 帧编辑 | 用户可修改帧描述和关联实体 |

#### 帧字段

| 字段 | 说明 |
| --- | --- |
| 帧序号 | 画面顺序编号 |
| 所选剧幕 | 当前帧来源的剧幕 ID 和剧幕标题 |
| 所属剧本 | 当前帧来源的剧本 ID 和剧本标题 |
| 原文片段 | 该帧来源于剧本中的哪段文字 |
| 画面描述 | 可直接用于 AI 画面生成的文字描述 |
| 镜头语言 | 景别、角度、镜头运动 |
| 情绪氛围 | 紧张、温暖、孤独、明亮等 |
| 关联实体 | 该帧出现的人物、物品、场景 |
| 参考图片 | 关联实体的模型图 |

#### 分帧范围规则

| 范围 | 说明 |
| --- | --- |
| 单选剧幕 | 只对一个剧幕生成分帧，适合分段制作和局部迭代 |
| 多选剧幕 | 对多个选中的剧幕按剧幕顺序生成连续分帧 |
| 全选剧幕 | 对项目下所有剧幕按顺序生成分帧 |

不同剧幕选择组合生成的分帧需要分别保存，避免用户在局部迭代时覆盖其他范围的分帧。

### 7.7 素材包导出页/步骤

页面目标：用户单选、多选或全选剧幕后，按所选剧幕导出可在其他 AI 平台继续使用的结构化素材。

#### 导出范围

| 范围 | 说明 |
| --- | --- |
| 单选剧幕 | 只导出一个剧幕对应的帧目录和相关实体图片 |
| 多选剧幕 | 导出多个选中剧幕对应的帧目录和相关实体图片 |
| 全选剧幕 | 导出项目下所有剧幕对应的帧目录和相关实体图片 |

#### 导出内容

| 内容 | 格式建议 |
| --- | --- |
| 原始剧本 | `.txt` 或 `.md`，导出所选剧幕文本，同时可附带项目全部剧幕 |
| 知识图谱 | `.json` 和可视化截图 |
| 实体对象清单 | `.json` 或 `.csv` |
| 实体模型图片 | 按帧复制相关 `.png`、`.jpg` 或 `.webp` 图片 |
| 分帧描述 | `.md`、`.json` 或 `.csv` |
| 帧与实体图片关联关系 | `.json` |

#### 导出选项

- 导出所选剧幕完整素材包。
- 仅导出实体模型图片。
- 仅导出分帧提示词。
- 复制指定帧提示词。
- 复制指定实体提示词。

#### 导出目录结构

导出的素材包必须以帧为粒度组织。每个帧目录使用数字编号命名，并包含该帧场景描述和该帧相关的实体对象图片。

推荐 ZIP 结构：

```text
export.zip
├── manifest.json
├── project.json
├── scripts/
│   ├── current-script.md
│   └── all-scripts.md
├── knowledge-graph/
│   ├── graph.json
│   └── graph-snapshot.png
└── frames/
    ├── 001/
    │   ├── scene.md
    │   ├── frame.json
    │   └── entities/
    │       ├── character_xxx.png
    │       ├── object_xxx.png
    │       └── scene_xxx.png
    ├── 002/
    │   ├── scene.md
    │   ├── frame.json
    │   └── entities/
    │       └── character_xxx.png
    └── 003/
        ├── scene.md
        ├── frame.json
        └── entities/
            ├── character_xxx.png
            └── scene_xxx.png
```

`scene.md` 应包含：

- 帧编号。
- 所属剧本标题。
- 原文片段。
- 帧场景描述。
- 镜头语言。
- 情绪氛围。
- 关联实体清单。
- 可复制到外部平台的完整画面生成提示词。

`frame.json` 应包含机器可读数据：

- 帧 ID。
- 分帧范围。
- 所属项目 ID。
- 所属剧本 ID。
- 帧序号。
- 场景描述。
- 镜头语言。
- 情绪氛围。
- 关联实体 ID。
- 关联实体图片文件名。

### 7.8 项目页

页面目标：帮助用户创建、查看、重命名、删除和打开项目。

建议路径：`/projects`

#### 列表字段

| 字段 | 说明 |
| --- | --- |
| 项目名称 | 默认取剧本标题，可重命名 |
| 剧幕数量 | 项目下的剧幕数量 |
| 实体数量 | 已识别实体对象数量 |
| 已完成图片 | 已绑定参考图的实体数量 |
| 分帧数量 | 已生成的视频帧数量 |
| 状态 | 解析中、待确认实体、生成图片中、可导出、失败 |
| 更新时间 | 最近一次编辑时间 |
| 操作 | 创建、打开、重命名、删除 |

## 8. 核心组件拆分

| 组件 | 说明 |
| --- | --- |
| `AppShell` | 应用整体布局，包含顶部导航和页面容器 |
| `WorkflowSidebar` | 左侧步骤导航和项目完成度 |
| `ScriptEditor` | 剧本输入、字数统计、示例填充 |
| `AnalysisProgress` | 剧本解析、图谱生成、实体抽取进度 |
| `KnowledgeGraphCanvas` | 知识图谱可视化画布 |
| `GraphNodeDetailPanel` | 图谱节点详情与编辑 |
| `EntityList` | 实体对象列表 |
| `EntityCard` | 实体对象卡片 |
| `EntityEditor` | 实体名称、类型、描述编辑 |
| `PromptEditor` | 图片生成提示词编辑 |
| `ReferenceImageUploader` | 用户参考图片上传 |
| `EntityImageGallery` | 实体图片候选结果展示 |
| `FrameList` | 分帧列表 |
| `FrameEditor` | 帧描述和关联实体编辑 |
| `AssetExportPanel` | 素材包导出 |
| `ProjectList` | 项目列表 |
| `StatusBadge` | 项目、实体、帧的状态标签 |
| `EmptyState` | 空状态展示 |
| `ErrorState` | 错误状态展示 |

## 9. 数据模型草案

### 9.1 项目

```ts
type ProjectStatus =
  | 'draft'
  | 'analyzing'
  | 'graph_ready'
  | 'entities_confirming'
  | 'images_generating'
  | 'frames_ready'
  | 'export_ready'
  | 'failed';

interface Project {
  id: string;
  title: string;
  contentType?: string;
  status: ProjectStatus;
  progress: number;
  scriptCount: number;
  entityCount: number;
  completedEntityImageCount: number;
  frameCount: number;
  activeScriptId?: string;
  errorMessage?: string;
  createdAt: string;
  updatedAt: string;
}
```

### 9.2 剧本

```ts
type ScriptStatus = 'draft' | 'analyzing' | 'analyzed' | 'failed';

interface ProjectScript {
  id: string;
  projectId: string;
  title: string;
  content: string;
  contentType?: string;
  orderIndex: number;
  status: ScriptStatus;
  wordCount: number;
  errorMessage?: string;
  createdAt: string;
  updatedAt: string;
}
```

### 9.3 知识图谱

```ts
type GraphNodeType = 'character' | 'object' | 'scene' | 'event' | 'concept';

interface KnowledgeGraphNode {
  id: string;
  type: GraphNodeType;
  name: string;
  description: string;
  sourceText?: string;
  entityId?: string;
  sourceScriptIds: string[];
  changeState?: 'existing' | 'new' | 'updated' | 'merge_pending';
}

interface KnowledgeGraphEdge {
  id: string;
  sourceNodeId: string;
  targetNodeId: string;
  relation: string;
  description?: string;
  sourceText?: string;
  sourceScriptIds: string[];
}

interface KnowledgeGraph {
  nodes: KnowledgeGraphNode[];
  edges: KnowledgeGraphEdge[];
}
```

### 9.4 实体对象

```ts
type EntityType = 'character' | 'object' | 'scene' | 'concept';
type EntityStatus =
  | 'pending'
  | 'confirmed'
  | 'ignored'
  | 'image_pending'
  | 'image_ready'
  | 'merge_pending';

interface Entity {
  id: string;
  projectId: string;
  name: string;
  type: EntityType;
  description: string;
  visualDescription?: string;
  prompt?: string;
  status: EntityStatus;
  mainImageId?: string;
  imageIds: string[];
  sourceScriptIds: string[];
  sourceScriptCount: number;
  occurrenceCount: number;
  mergeCandidateIds?: string[];
  createdAt: string;
  updatedAt: string;
}
```

### 9.5 实体图片

```ts
interface EntityImage {
  id: string;
  entityId: string;
  imageUrl: string;
  prompt: string;
  referenceImageUrl?: string;
  isMain: boolean;
  createdAt: string;
}
```

### 9.6 视频帧

```ts
type FrameScope = 'current_script' | 'all_scripts';

interface VideoFrame {
  id: string;
  projectId: string;
  scriptId?: string;
  scriptTitle?: string;
  scope: FrameScope;
  index: number;
  sourceText: string;
  description: string;
  camera?: string;
  mood?: string;
  entityIds: string[];
  referenceImageIds: string[];
  prompt: string;
}
```

### 9.7 导出记录

```ts
type ExportScope = 'current_script' | 'all_scripts';

interface ExportRecord {
  id: string;
  projectId: string;
  scriptId?: string;
  scope: ExportScope;
  fileUrl: string;
  frameCount: number;
  createdAt: string;
}
```

## 10. 接口预留

| 接口 | 方法 | 用途 |
| --- | --- | --- |
| `/api/projects` | `POST` | 创建项目，可同时保存首个剧本 |
| `/api/projects` | `GET` | 获取项目列表 |
| `/api/projects/:projectId` | `GET` | 获取项目详情 |
| `/api/projects/:projectId` | `PATCH` | 重命名项目 |
| `/api/projects/:projectId` | `DELETE` | 删除项目及其本地素材 |
| `/api/projects/:projectId/scripts` | `POST` | 在项目下新增剧本 |
| `/api/projects/:projectId/scripts` | `GET` | 获取项目下的剧本列表 |
| `/api/projects/:projectId/scripts/:scriptId` | `PATCH` | 更新剧本标题或内容 |
| `/api/projects/:projectId/scripts/:scriptId` | `DELETE` | 删除指定剧幕 |
| `/api/projects/:projectId/analyze` | `POST` | 解析全部剧幕，重新生成或更新项目级知识图谱和实体 |
| `/api/projects/:projectId/graph` | `GET` | 获取知识图谱 |
| `/api/projects/:projectId/entities` | `GET` | 获取实体列表 |
| `/api/entities/:entityId` | `PATCH` | 更新实体信息 |
| `/api/entities/:entityId/merge` | `POST` | 将实体合并到另一个实体 |
| `/api/entities/:entityId/images` | `POST` | 为实体生成图片 |
| `/api/entities/:entityId/reference-images` | `POST` | 上传实体参考图片 |
| `/api/projects/:projectId/frames/generate` | `POST` | 按选中剧幕生成视频分帧描述 |
| `/api/projects/:projectId/frames` | `GET` | 获取分帧列表 |
| `/api/frames/:frameId` | `PATCH` | 更新帧描述和关联实体 |
| `/api/projects/:projectId/export` | `POST` | 按选中剧幕导出帧级素材包 |

分帧接口请求参数示例：

```json
{
  "script_ids": ["script_001", "script_002"]
}
```

导出接口请求参数示例：

```json
{
  "script_ids": ["script_001", "script_002"]
}
```

## 11. 任务状态与进度反馈

| 阶段 | 用户可见状态文案 |
| --- | --- |
| 保存剧本 | 正在保存剧本 |
| 增加幕 | 正在将剧幕加入当前项目 |
| 剧本解析 | 正在分析全部剧幕结构 |
| 实体抽取 | 正在识别人、物、场景等实体对象 |
| 实体合并 | 正在与项目已有实体进行匹配和合并 |
| 图谱生成 | 正在更新项目级知识图谱和对象关系 |
| 图片生成 | 正在为实体对象生成参考图片 |
| 分帧生成 | 正在按选定范围拆分视频帧并生成画面描述 |
| 导出素材 | 正在按帧目录整理素材包 |

## 12. 错误与异常处理

| 场景 | 处理方式 |
| --- | --- |
| 剧本为空 | 在输入区下方提示“请输入文字剧本” |
| 剧本过短 | 提示“建议至少输入 100 个字，以便识别完整关系” |
| 解析失败 | 展示失败原因，允许重新解析 |
| 新增剧本后实体重复 | 标记为“待确认合并”，允许用户合并或保留 |
| 图谱节点过多 | 默认折叠低优先级节点，并提供筛选 |
| 实体识别错误 | 允许用户编辑、忽略或新增实体 |
| 图片生成失败 | 展示失败原因，允许调整提示词后重试 |
| 参考图片上传失败 | 提示文件格式、大小或网络问题 |
| 分帧生成失败 | 允许重新生成，并保留已确认实体资产 |
| 未选择剧幕 | 分帧和导出时提示用户至少选择一幕 |
| 导出失败 | 展示失败原因，允许重新导出 |

## 13. PC 端体验要求

- 工作台需要突出流程感，让用户清楚当前处于哪个步骤、下一步要做什么。
- 项目工作台需要明确展示当前项目和项目下的剧幕列表。
- 修改剧本标题后，剧幕列表内容需要实时同步。
- 知识图谱画布需要支持缩放、拖拽、搜索和节点筛选。
- 实体对象列表需要适合批量处理，优先使用紧凑卡片或表格。
- 图片生成流程需要始终保留实体描述、提示词和参考图上下文。
- 分帧页面需要让用户单选、多选、全选剧幕，并快速复制单帧提示词，同时看到关联的实体模型图。
- 导出页面需要在导出前明确展示所选剧幕、帧数量和涉及实体图片数量。
- 页面状态切换时需要保持主要工作区尺寸稳定，避免频繁跳动。
- 删除项目、删除实体图片等危险操作需要二次确认。

## 14. 首版开发优先级

### P0

- 项目工作台基础布局。
- 项目管理，支持项目创建、删除、重命名。
- 项目级剧幕管理，支持增加幕、删除幕、编辑标题和正文。
- 全部剧幕解析任务提交。
- 解析进度展示。
- 项目级知识图谱展示。
- 实体对象列表与实体详情编辑。
- 实体图片生成流程。
- 按所选剧幕生成分帧并展示。

### P1

- 用户上传实体参考图片。
- 实体图片候选结果预览与主图选择。
- 帧描述编辑与实体关联调整。
- 按所选剧幕导出素材包。
- 按数字帧目录组织导出素材。
- 项目列表。

### P2

- 图谱节点高级筛选。
- 实体批量确认和批量忽略。
- 疑似重复实体批量合并。
- 提示词模板。
- 多格式导出。
- 外部平台提示词格式适配。

## 15. 待确认问题

- 首版是否需要登录态。
- 知识图谱可视化使用前端本地布局，还是由后端返回节点坐标。
- AI 图片生成接口由平台自研、第三方服务，还是先使用 mock。
- 实体对象是否需要支持多张主参考图，例如正面、侧面、半身、全身。
- 新增剧本后实体自动合并的置信度阈值如何设定。
- 分帧粒度按镜头、按画面、按句子，还是由用户选择。
- 导出的素材包首版优先支持 `.json`、`.md`、`.csv` 中的哪些格式。
- 是否需要为不同外部平台生成不同提示词版本。
- 按部分剧幕导出时，是否需要同时附带项目级完整知识图谱。
- 剧幕顺序是否支持手动拖拽调整。
